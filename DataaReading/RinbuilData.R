#Data that comes with R and other packages in the R
#Ecosystem
#ggplot2, for instance, comes with a
#dataset about diamonds. It can be loaded using the 
#data function
data(diamonds, package = "ggplot2")
head(diamonds)

#make sure you install the ggplot2 package first
#To find a list of available data, simply type data() into the console.
